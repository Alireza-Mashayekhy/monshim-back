services:

  redis:
    container_name: monshim_redis_dev

    image: redis:7-alpine

    restart: unless-stopped

    ports:
      - "6381:6379"

    volumes:
      - redis_dev_data:/data


volumes:
  redis_dev_data: